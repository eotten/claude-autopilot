server {
    listen 80;
    server_name autopilot.yourdomain.com;

    # Redirect to HTTPS (uncomment after certbot runs)
    # return 301 https://$host$request_uri;

    location / {
        proxy_pass http://127.0.0.1:5055;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Long timeouts for streaming task output
        proxy_read_timeout 300s;
        proxy_connect_timeout 10s;
    }
}

# HTTPS block — certbot will generate this, or uncomment and fill in:
# server {
#     listen 443 ssl;
#     server_name autopilot.yourdomain.com;
#
#     ssl_certificate /etc/letsencrypt/live/autopilot.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/autopilot.yourdomain.com/privkey.pem;
#
#     location / {
#         proxy_pass http://127.0.0.1:5055;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 300s;
#         proxy_connect_timeout 10s;
#     }
# }
